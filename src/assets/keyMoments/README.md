# Key Moments — downloaded video assets

Videos fetched by the **Mission: AI Possible → Key Moments** admin tab are
stored in this directory by the server (`server/services/keyMomentsService.js`)
and served at `/assets/key-moments/<filename>`.

Do not commit the binary `.mp4` files — they are regenerated on demand.
